# Postgres (local Docker Compose — see docker-compose.yml)
DATABASE_URL="postgresql://rag_chatbot:rag_chatbot@localhost:5433/rag_chatbot"

# Google Gemini (https://aistudio.google.com/apikey) — free tier, no billing required
GEMINI_API_KEY=""
GEMINI_EMBEDDING_MODEL="text-embedding-004"
GEMINI_GENERATION_MODEL="gemini-2.0-flash"
